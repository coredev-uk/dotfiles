[user]
	email = core@coredev.uk
	name = Core
	signingkey = 2AB8327FBA02D1C0
[credential]
	helper = /usr/lib/git-core/git-credential-libsecret
[core]
	excludesFile = ~/.gitignore_global
	autocrlf = input
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[pull]
	rebase = false
{% if yadm.os == "Linux" %}
[commit]
	gpgsign = true
{% else %}
[commit]
	gpgsign = false
{% endif %}
