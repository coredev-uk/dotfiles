[user]
	email = 64542347+coredev-uk@users.noreply.github.com
	name = Core
	signingkey = FE9BF1B547F8F3C6
[credential]
	helper = /usr/lib/git-core/git-credential-libsecret
[core]
	excludesFile = ~/.gitignore_global
	autocrlf = input
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[pull]
	rebase = false
{% if yadm.os == "Linux" %}
[commit]
	gpgsign = true
{% else %}
[commit]
	gpgsign = false
{% endif %}
