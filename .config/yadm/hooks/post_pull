#!/bin/sh

# firefox
if [[ -c firefox ]]; then 
	for path in ~/.mozilla/firefox/*-release/; do
		mkdir $path/chrome/ 2&>/dev/null
		cp ~/.config/firefox/* $path/chrome
	done
fi

# Windows
if [[ -c efibootmgr ]]; then
	sudo cp ~/.scripts/bootwin /usr/local/bin
fi

# Pull SSH
cd ~/.ssh
git pull
